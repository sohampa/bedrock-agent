# Source before deploy on Windows (PowerShell):
#   Get-Content agentcore\.env.deploy | ForEach-Object { if ($_ -match '^([^#=]+)=(.*)$') { Set-Item -Path "env:$($matches[1])" -Value $matches[2] } }
UV_LINK_MODE=copy
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1
